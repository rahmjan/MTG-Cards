name: mtgCards-compose

services:
    db:
        image: postgres:17.5-alpine
        ports:
            - "5432:5432"
        environment:
            POSTGRES_USER: admin
            POSTGRES_PASSWORD: admin
            POSTGRES_DB: mtg
